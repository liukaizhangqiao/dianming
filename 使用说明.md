# 随机点名器使用说明

## 一、打开方式

双击 `index.html`，使用 Edge 或 Chrome 浏览器打开即可。

- 无需安装软件
- 无需联网
- 学生名单、页面效果和点名功能均包含在文件中

### 在 iPhone 上使用

1. 将整个项目发布到支持 HTTPS 的静态网站（例如 GitHub Pages）。
2. 用 iPhone 的 Safari 打开发布后的网址。
3. 点击 Safari 的“分享”按钮，选择“添加到主屏幕”。
4. 开启“作为 Web App 打开”，然后点击“添加”。
5. 从主屏幕打开一次点名器，并保持联网直到页面完整显示。

首次成功打开后，网页文件会缓存在设备上，之后断网时也可以继续使用。名单保存在当前浏览器或主屏幕 Web App 中，不会自动同步到其他设备。

## 二、主要功能

### 1. 随机点名

点击底部的“启动点名”，学生姓名会在星空背景中快速闪动。

再次点击“立即停止”，或按键盘空格键，即可停止点名。抽中的姓名会直接显示在页面中央。

### 2. 设置抽取人数

点击“人数”旁边的 `−` 和 `＋`，可设置一次抽取的人数。

支持一次随机抽取 1～10 人。

### 3. 重复模式

- **可重复**：已经抽中过的学生仍可再次被抽中。
- **不可重复**：本轮已经抽中过的学生不会再次被抽中。

不可重复模式剩余人数不足时，系统会自动开始新一轮。

### 4. 姓名朗读

停止点名后，系统会自动朗读抽中的姓名。多人模式会按照中央编号依次朗读。

点击底部的 `♪` 按钮可以打开或关闭提示音和姓名朗读。

> 朗读音色由电脑系统自带的中文语音决定。

### 5. 修改名单

点击底部右侧的 `☰` 按钮进入名单管理。

- 每行填写一个姓名
- 点击“保存”应用新名单
- 点击“恢复默认”恢复原始班级名单

修改后的名单会自动保存在当前浏览器中。

### 6. 返回姓名页面

点名结果显示在中央后，点击“返回”即可收起结果，重新显示完整姓名页面。

## 三、快捷键

- `空格键`：启动或停止点名
- `Esc`：停止点名、收起结果或关闭名单管理

## 四、分享给其他人

可以直接发送 `index.html` 文件。建议先压缩成 ZIP 再通过微信、QQ 或邮箱发送。

对方下载并解压后，双击 `index.html` 即可使用。

如果需要在 iPhone 上使用，请分享发布后的 HTTPS 网址，不要只发送 `index.html` 文件。

## 五、使用建议

- 推荐使用最新版 Edge 或 Chrome 浏览器
- 课堂投影时建议开启电脑声音
- 首次播放声音时，需要先点击一次“启动点名”按钮
- 如果浏览器没有中文语音，姓名朗读可能无法正常播放
